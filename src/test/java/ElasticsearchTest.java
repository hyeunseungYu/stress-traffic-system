/*
import com.project.stress_traffic_system.product.model.Product;
import com.project.stress_traffic_system.product.repository.ProductElasticRepository;
import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

import javax.annotation.Resource;

@SpringBootTest
public class ElasticsearchTest {

    @Resource
    ProductElasticRepository productElasticRepository;

    @Test
    void test() {
        Product product = new Product();
        product.setName("상품이름");
        product.setStock(100);
        product.setOrderCount(0L);
        product.setPages(200);
        product.setIntroduction("책 소개글 ~~~~~");
        product.setDescription("책 상세설명 !!!!");

        productElasticRepository.save(product);
    }
}
*/
